# 職業訓練「グラフィック・Webデザイナー養成科」におけるJavaScriptの授業(8/19,20,21)のカリキュラム策定

## 受講生情報
- グラフィック及びWebデザイナーへのキャリアチェンジを目指す職業訓練生。
- スキルレベルは現時点で不明だが、プログラミング未経験者であることを前提とする。
- JS の授業前までに基本的な HTML/CSS については学習済みの予定。習熟度は低めの前提とする。
- 本クラスはプログラミング科ではなく Webデザイナー向けのため、複雑なUIやロジックの実装は目標に含めない。

## 訓練情報
- 1コマ50分×6コマ / 1日
- トータルで3日間(8/19,20,21)
- 対面授業
- Google Chrome, Visual Studio Code

## 着地点（カリキュラムのゴール）

静的な1ページサイト（ポートフォリオ）に、案件でよく使われる動きを **3パターン** 付ける。About・Skills は静的セクションとして HTML/CSS のみで掲載する。

| 優先度 | UI | 概要 |
|--------|-----|------|
| 必須 | ドロワーメニュー | ハンバーガーで開閉。クラスの付け外し。**PC・SP ともドロワー**（メディアクエリで切替しない） |
| 必須 | モーダル（画像拡大） | Works カードクリック → `<dialog>` で画像拡大（`src` コピー） |
| ライブラリ1つ | カルーセル（Swiper） | CDN 読み込み＋数行の初期化。自作はしない |

※ アコーディオン・FAQ は**不採用**（ポートフォリオの UX・文脈に合わない。詳細は `docs/design_brief.md`）。

### 対象外（講師デモに留めるか省略）
- タブメニュー
- アコーディオン UI
- `aria-*` 属性（JS 実装に集中するため教材では付けない。口頭で触れる程度）
- 配列データからの HTML 動的生成・フィルタリング・再描画（プログラミング寄りすぎる）
- イベント委譲（イベントバブリング、`event.target` の判別など）。口頭または短いデモで「こう書く方法もある」程度に留める

### jQuery の立ち位置

**紹介のみ。最終課題・演習の実装には使わない。**

| 項目 | 内容 |
|------|------|
| 位置づけ | 現場で見かける用語・書き方の**概要説明**（レガシーサイト・古いテンプレート文脈） |
| 実施タイミング | **3日目・4コマ目**（約10〜15分のデモ＋説明。個人実装はしない） |
| 最終課題との関係 | `materials/finish/` および各 `steps/` は **バニラ JS のみ**。jQuery は読み込まない |
| 着地点 | 「`$` や `$(function(){})` を見たことがある」「バニラ JS の `addEventListener` と役割が似ている」とわかれば十分 |

**やること（紹介の範囲）**

- 「昔からよく使われた JS の書き方」として位置づける
- `$('.menu-btn').on('click', function(){ ... })` を**見本で見せる**（バニラ JS のドロワーコードと並べて対比）
- `$(function(){ ... })` ＝ ページ読み込み後に実行する、という意味だけ伝える
- 新規制作では Vanilla JS やフレームワークが主流であることを一言添える

**やらないこと**

- jQuery でドロワー・モーダル等を**実装させる**
- セレクタ詳細、`.each()`、AJAX、プラグインの自作・深い設定
- 構文の暗記や演習課題

※ 初版目標の「jQuery の基本構文がわかる」は、本クラスでは **上記の概要レベル** で充足するものとする。

### バニラJSで扱う範囲
- `querySelector` / `querySelectorAll` / `addEventListener`
- `classList.add` / `remove` / `toggle`
- モーダル：`showModal()` / `close()`（`<dialog>`）。画像の `src` / `alt` をカード内 `img` からコピー
- Works 複数カード：`querySelectorAll` + `forEach`（3日目・進捗に応じて）
- `data-*` 属性・配列・`innerHTML` による動的生成は着地点に含めない

### 複数要素へのイベント登録（Works・3日目）

Works カードが複数あるため、1日目の「要素1つ＋クリック」を発展させる。アコーディオンではなく**全カードに同じモーダル処理**を付ける。

| 方式 | 扱い | 理由 |
|------|------|------|
| `querySelectorAll` + `forEach` | **教える（3日目）** | 各カードの `img.src` を dialog にコピーして開く |
| 配列 / `data-*` | 対象外 | カード内の `img` から `src` を読めば足りる |
| イベント委譲 | 参考程度 | 説明コストが高い |

運用上は、決まり文句としてパターンを配布し、class 名の変更など一部を自分で直す形から始める。全員が自力で書けることは目標にしない。

## 目標

### 受講生向け（着地点の言い換え）
- 静的な1ページサイトに、ドロワーメニュー・モーダル（画像拡大）を実装できる（2日目）。
- 3日目に、Works 全カードへの `forEach` と Swiper を**進捗に応じて**追加できる。
- 必要に応じて、カルーセルをライブラリ（Swiper）で追加できる。
- jQuery は概要のみ知り、本課題の実装はバニラ JS で行う。
- 「クリックで見た目が変わる」仕組みを、自分のデザインに当てはめられる。

### 到達レベルの切り分け
| レベル | 到達イメージ | 目安 |
|--------|--------------|------|
| A（最低ライン） | 完成例を動かし、テキスト・色・画像を差し替えられる。コードの役割をざっくり説明できる | 全員 |
| B（本来の着地点） | ドロワー＋モーダル（1カードで画像拡大）を組み込める（2日目） | 約1/3 |
| C（余力） | 全カードで画像拡大（`forEach`）＋ Swiper | 数名＋講師デモ |

※「全員が3動き全部を自力完成」は目標にしない。

## 3日間の大枠

| 日 | 内容 | 1日のゴール |
|----|------|-------------|
| 1日目（8/19） | 導入・Webの仕組み復習 → JS入門 → **ドロワーメニュー完成** | 「クリックで class が変わる」体験を得る |
| 2日目（8/20） | 前日の型の応用 → **モーダル**に集中 | モーダルが動く（Bレベルの核） |
| 3日目（8/21） | Works 全カード（`forEach`）→ Swiper → 仕上げ | ライブラリ＋総仕上げ |

---

## 各日のスケジュール（案）

※ 1コマ＝50分。3コマ目と4コマ目の間に昼休みがある想定。

### 1日目（8/19）── 導入とドロワーメニュー

初日は「プログラミング＝難しい」という先入観を和らげつつ、**午後には必ず手が動く成功体験**を作る。3コマ目以降のペースが初日の印象を決める。

| コマ | テーマ | 内容 | ねらい・メモ |
|------|--------|------|--------------|
| **1** | オリエンテーション | 講師自己紹介 / 「プログラム」とは（運動会・コンサートのプログラム＝**手順・設計図**のたとえ） / コンピュータプログラムとは（言語ごとの得意分野：HTMLは構造、CSSは見た目、**JSは動き** など） | いきなりコードを書かせない。デザイナーが「自分ごと」に感じられる導入 |
| **2** | Webの仕組み（復習） | サーバ・クライアントの復習 / ブラウザが URL をリクエスト → レスポンス受信 → **HTML解析 → CSS適用 → JS実行 → 描画** の流れ | HTML/CSS 学習の延長として位置づける。「JS はブラウザの中で動く」と伝える土台 |
| **3** | JavaScript とは | HTML・CSS・JS の役割分担（家のたとえ等） / ブラウザと VS Code の役割 / `script` タグの読み込み / **DevTools の Console** を開く / `console.log('Hello')` を全員で実行 | 初めての「書いて動いた」体験。エラーが出ても慌てない雰囲気づくり |
| **4** | DOM と要素の取得 | DOM とは（HTML をブラウザがツリー状にしたもの、程度の説明） / `document.querySelector` で要素を1つ取る / 配布サイトの HTML 構造を一緒に確認 / ボタンクリックで `console.log` するまで | 「名前（セレクタ）で部品を指定する」感覚をつける。変数は必要最低限 |
| **5** | クリックと class 操作 | `addEventListener('click', ...)` の型 / `classList.add`・`remove`・`toggle` / 配布 HTML のドロワー用マークアップと CSS（`.is-open` 等）を確認 / **講師デモ：ボタン1つでメニューが開く** | 1日目の核心。「JS は見た目の状態（class）を切り替える」という型を示す |
| **6** | ドロワーメニュー実装 | 講師と一緒に追記 → 個人ワーク（TA が巡回） / 動いた人は閉じるボタン・オーバーレイクリックにも挑戦（任意） / **1日目のまとめ**：「今日やったことは全部、クリック→class切替」 | **初日の着地点**。動かなくても完成例の動作確認とコード読みは A レベルとして OK |

**1日目で扱わないこと**（混乱を避けるため）：変数の深い説明、関数の書き方の解説、`forEach`、イベントオブジェクト、jQuery。

---

### 2日目（8/20）── モーダル

1日目と**同じ型**（要素を取る → クリックを待つ → 表示を変える）であることを繰り返し明示する。  
**モーダル1つに集中**し、仕上げ・質問の余裕を確保する（案A）。

| コマ | テーマ | 内容 | ねらい・メモ |
|------|--------|------|--------------|
| **1** | 復習 | 1日目のドロワーコードを短く振り返り / 質問タイム / 今日の流れ（モーダル）の提示 | 遅れている受講生向けに完成例を再配布 |
| **2** | モーダル（仕組み） | `<dialog>` / `showModal()`・`close()` / Works カードと `#works-dialog` の HTML を確認 | カード上に情報、dialog は画像のみ |
| **3** | モーダル（実装①） | 講師デモ：**1枚のカード**クリック → カード内 `img.src` を `#works-dialog-img` にコピー → 開く | `src` は「画像の住所をコピー」と説明 |
| **4** | モーダル（実装②） | 個人ワーク（TA 巡回） / 閉じるボタン | **B レベル到達のチェックポイント** |
| **5** | モーダル仕上げ | 未完了者フォロー / About・Skills セクションを確認（静的・JS不要） | 明日は全カードに同じ処理を繰り返す |
| **6** | 2日目のまとめ | 「取得→クリック→（画像の住所をコピー）→表示」の確認 | 2日目終了時点で1カードが動けば十分 |

---

### 3日目（8/21）── Works 全カード・ライブラリ・仕上げ

午前は `forEach` で全カード対応、午後は Swiper と総仕上げ。**全カード対応は進捗を見て深さを調整**（下記参照）。

| コマ | テーマ | 内容 | ねらい・メモ |
|------|--------|------|--------------|
| **1** | Works 全カード | `querySelectorAll('.works-card')` + `forEach` / 各カードの `img.src` をコピーして開く | **遅れていればデモ＋`finish` 確認のみ** |
| **2** | Works 実装 | 講師デモ → 個人ワーク（進捗に応じて） | カードごとに異なる画像が拡大されれば成功 |
| **3** | ライブラリ・Swiper | CDN 読み込み / 最小限の初期化 | バニラ JS 未完了でも Swiper で成功体験 |
| **4** | jQuery（紹介のみ） | ドロワーと並べて見本表示（10〜15分） | 着地点に含めない |
| **5** | 総合仕上げ | 通し確認 / カスタマイズ / TA フォロー | A レベル全員が何か変えられるように |
| **6** | まとめ・質疑 | 3日間の振り返り / 質疑 | — |

#### Works 全カード（forEach）の進め方

| レベル | 条件の目安 | やること |
|--------|------------|----------|
| **最小** | 2日目のモーダルで多くがつまずいている | `finish` で全カード動作をデモのみ |
| **標準** | 1カード版が動いている | `forEach` の決まり文句を一緒に追記 |
| **十分** | 余裕がある | 個人ワークまで |

※ 3日目の着地点は **Swiper ＋総仕上げ** を優先。

---

### スケジュール設計上の方針

- **初日3コマ目が分水嶺**：1〜2コマ目は通例どおり概念導入に充て、**3コマ目で必ず Console に手を動かす**。
- **毎日最終コマは実装・まとめ**：座学が続くと集中力が切れるため、各日の6コマ目（または5〜6コマ目）はワーク中心にする。
- **2日目はモーダル1本**：Bレベル到達（ドロワー＋モーダル）を優先し、負荷を分散（案A）。
- **3日目の Works 全カード（forEach）は可変**：進捗に応じてデモのみ〜個人ワークまで。Swiper・仕上げを優先。
- **同じ型の反復**：1日目で確立した「取得→クリック→class」の型を、2日目以降は新しい UI 名を当てはめるだけに見せる。
- **TA の役割**：4コマ目以降は巡回・個別フォローが主。セレクタの typo、JS ファイルの読み込み忘れ、`script` の位置など定型的なつまずきに備える。

## 懸念点
- デザイナー志望の方が多いクラスで、IllustratorやPhotoshopを扱う時間が長いので、苦手な受講生が多いのが通例。1/3程度の受講生がある程度理解できれば御の字という考え。
- UIパターンを増やしすぎると混乱するため、着地点は「1サイト・3動き（＋静的 About/Skills）」に絞る。

## 補足情報
- 本講座は新人講師向けのモデル授業。TAとして二人（出野先生・辻野先生）がアサインされている。
- 新人講師には本講座を再現できることが求められている（聴講・TA 同行は 8/19～21 の範囲で実施）。
- 8/25 の育成講師向けフォローアップ座学は、報酬不成立により**不実施（白紙）**。詳細は `docs/trainer_workshop_brief.md`。
- **現時点の策定範囲は 8/19～21 の3日間のみ。**
- ベースサイト・最終課題の具体化 → `docs/final_project.md`（逆算カリキュラム）
- コンテンツ・トンマナ → `docs/design_brief.md`（グレースケール・ワイヤーフレーム調）
- **`materials/finish/`** … HTML 完了。CSS は Swiper 除き完了（2026-07-10）。JS 未着手
