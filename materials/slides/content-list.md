# スライド コンテンツリスト

> `docs/agenda.md` の18コマをもとにした構成案。実際のスライド(`slide.md`)を作る前の確認用。
> ルール: `CLAUDE.md`(表紙→中表紙→本文、1ページ1コンテンツ、10行以内、header+contentsマークアップ)

## 表紙

- タイトル / サブタイトル / 作成日 / 作成者

---

## セクション1: JavaScriptとは

**中表紙**: JavaScriptとは

1. 「プログラム」とは（運動会・コンサートのプログラム＝手順・設計図のたとえ）
2. コンピュータプログラムとは（HTMLは構造、CSSは見た目、JSは動き）
3. 様々なプログラミング言語
4. Webの仕組み（復習）― サーバー・クライアント
5. リクエストからレンダリングまでの流れ（HTML解析→CSS適用→JS実行→描画）
6. JavaScriptとは（HTML・CSS・JSの役割分担）※作成済み
7. `script`タグの読み込み
8. はじめてのconsole.log ※作成済み（コードブロック例）

---

## セクション2: 要素(DOM)の操作

**中表紙**: 要素(DOM)の操作

9. DOMとは（概要）
10. DOMを操作する手順(取得・プロパティの確認・書き換えをまず紹介)
11. DOMの取得 （`querySelector`）
12. プロパティの確認と書き換え（`innerText`・`src`・`alt`・`href`）
13. イベントとは（概要）
14. イベントの型（クリックを待つ）
15. class属性の操作 (`classList`)
16. `classList`の操作（add / remove / toggle）
17. ドロワーのHTML/CSS確認（`.is-open`）
18. `drawer.js`を書き始める（型のおさらい：取得→クリック→class）
19. ドロワー実装（個人ワーク）
20. DOMの操作のまとめ

---

## セクション3: モーダル

**中表紙**: モーダル

21. DOMの操作の振り返り（同じ型の反復であることを明示）
22. モーダルとは
23. `<dialog>`とは
24. モーダルの開閉（`showModal()` / `close()`）
24.5. 変数（`const`）― 要素を名前で使い回す（教材設計上、変数はここで初導入）
25. 画像のコピー（`img.src` / `img.alt`）
26. モーダル実装（1カード）
27. 閉じるボタンの実装
28. モーダルのまとめ（取得→クリック→画像コピー→表示）

---

## セクション4: Works全カードの実装

**中表紙**: Works全カードの実装

28a. `querySelectorAll()`とは（すべて取得・リストになる）
28b. `forEach()`とは（リストを1つずつ繰り返す型）
29. Works全カード対応の実装

---

## セクション5: ライブラリ

**中表紙**: ライブラリ

30. ライブラリとは（Swiper・CDN読み込み）
31. Swiperの初期化
32. jQueryの紹介（ドロワーと並べて見本表示、概要のみ）

---

## セクション6: 総仕上げ

**中表紙**: 総仕上げ

33. 総合仕上げ・カスタマイズ
33a. カスタムプロパティ（おまけ）
33b. CSSのネスト（おまけ）
34. 総まとめ

---

## メモ

- 「何日目」の表記はスライド内に出さない（CLAUDE.mdルール）
- 上記の1項目 = おおよそ1スライド想定（内容量によっては複数ページに分割）
- ミニ練習(`practice/`)の詳細操作手順はスライドに含めず、口頭・画面共有で対応する想定
